# 2026-04-17

Summary: Continued Raven API + SQLite learning session, passed the first data-shape artifact challenge, and promoted VIBE_DOCING into the general learning/help flow

## Activity

## [2026-04-17] LEARN | Raven SQLite data shape
Project: raven
- Loaded vault startup context for the new day and continued `learning/sessions/raven-api-sqlite-ingest-wire-2026-04-16.md`.
- User completed the first SQLite workbench data-shape artifact for `search_runs`, `search_queries`, and `source_candidates`.
- Recorded the artifact attempt and classified Gap 1 as passed with corrections: `query_index` is generated-query order, `created_at` is Raven storage time, `published_at` is source publication time, and one candidate row represents one normalized result.
- Checked local Codex config for Obsidian MCP access. The Raven project is already trusted and the Obsidian MCP server points at `/mnt/d/ANHDUC/ADUC_vault/ADUC`; no per-call approval flag was visible in the config.

## [2026-04-17] AUDIT | Raven SQLite schema attempt
Project: raven
- User attempted the first Raven SQLite schema for `search_runs`, `search_queries`, and `source_candidates`.
- Recorded the SQL attempt in `learning/sessions/raven-api-sqlite-ingest-wire-2026-04-16.md`.
- Classified Gap 2 as failed but structurally useful: user understands the three-table split and key intent, but needs to fix SQLite syntax, table responsibility, foreign-key references, candidate ID types, raw metadata placement, and duplicate protection.
- User produced a second schema attempt that fixed the basic table split and `target`, then correctly pushed back on raw result placement: full API responses belong to the API-call/query layer, while candidate raw item metadata is a sliced per-result debug artifact.
- User then objected that enriched YouTube and Reddit queries are unique after enrichment, so `search_queries` can mean one platform-specific search call with `source` and raw response attached. Accepted with condition: candidate ownership should still flow through `query_id`, not through a foreign key to `source`.
- User revised the schema to remove the bad candidate-source foreign key, add `status_code` and `raw_response`, and keep `(source, platform_id)` as the candidate dedupe lock. Classified Gap 2 as passed for Phase 1 architecture; next gap is fake SQLite insertion/contract verification before API calls.
- User flagged that pure schema drills were not working. Added a visual sandbox ladder to the learning session: S0 visual trace -> S1 SQLite sandbox -> S2 fake Raven module -> S3 query enricher sandbox -> S4 YouTube -> S5 Reddit -> S6 Phase 1 search function.
- User narrowed the method to SQLite -> YouTube -> Reddit -> one actual search into database. Created `scratch/sqlite_workbench_demo.py` as a throwaway S1 sandbox, ran it successfully, and verified row counts, joined candidate reconstruction, duplicate result rejection, and fake `query_id` rejection.
- User pushed back that passive sandbox tracing still was not the right teaching path. Updated the learning session with a builder-first method: Duc builds progressively from the simplest real SQLite database upward; agent audits, provides official docs/reference after attempts, and labels true missing-knowledge interventions as `I will do this` rather than spoonfeeding direction.
- User formalized `vibe docing`: official-doc-shaped mechanism slices scoped to one operation, using neutral placeholder values instead of challenge-specific answers. Added the rule to the Raven learning session as an approved mentor operation.

## [2026-04-17] VAULT | VIBE_DOCING operation flow
Project: learning
- User requested vault keeping for `vibe docing` as a general operation, then asked to check the critical learn/help flow.
- Created `wiki/vibe-docing.md` as the canonical protocol: official-doc-shaped mechanism slices, neutral placeholders, no challenge-specific artifact answers.
- Registered `[[wiki/vibe-docing]]` in `index.md` and wired it into `wiki/learning-protocol-hub.md`, `wiki/help-protocol.md`, `wiki/pre-wire-protocol.md`, `learning/README.md`, vault `AGENTS.md`, and `CLAUDE.md`.
- Flow check result: Help Protocol now chooses VIBE_DOCING for narrow DOC_GAP/MECHANISM_GAP after the seam is visible; Pre-Wire uses it inside artifact challenges; large architecture gaps still route back to blueprint instead of docs.
