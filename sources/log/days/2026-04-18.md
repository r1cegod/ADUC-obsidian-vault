# 2026-04-18

Summary: Clarified Raven database learning flow and promoted Build-First Learning plus VIBE_DOCING into the general learning/help protocol stack

## Activity

## [2026-04-18] PLAN | Raven SQL-from-zero build flow
Project: raven
- User asked to rethink the current Raven database learning flow because he does not know SQL yet.
- Loaded the current learning/help protocols and active Raven API + SQLite learning session.
- Added `SQL-From-Zero Build Strategy` to `learning/sessions/raven-api-sqlite-ingest-wire-2026-04-16.md`: real Raven artifact first, one SQLite concept per artifact, vibe docing only for mechanism gaps, executable pass conditions, and deliberate breakage to derive rules.
- User revised the build rule: no more new scratch-folder learning artifacts for the Raven database track; build directly under `src/backend/`, and treat each artifact as a maturity level rather than a prebuild exercise.
- User built `src/backend/db_l0.py` and `src/backend/raven.sqlite`. Verified L0 passes: real SQLite file, table creation, insert, commit, select, and printed rows. Re-running appends duplicate placeholder rows, which is accepted for L0 and becomes the L1/L2 repeated-run behavior problem.
- User added `create_run(target)`, UTC timestamp generation, and `list_all_runs()` to `src/backend/db_l0.py`. Verified it runs and stores real rows. Classified DB2 as partial pass: function shape works, but the insert function should return the id and the list function should return rows instead of only printing before moving to query-table ownership.
- User proposed splitting Raven DB cases into normal cases and adaptive cases. Added the split to the learning session: normal cases are core database literacy to build before APIs; adaptive cases depend on real callers such as API adapters, retries, raw response pressure, migrations, rater/audit tables, or conflict resolution and should be built only when needed.
- User added all three backend database tables and asked to finish normal cases. Audited `src/backend/db_l0.py`; current blocker is N0 runnable schema initialization: script fails on invalid `ZoneInfo("Asia/ Ho_Chi_Minh")`, and next likely SQLite blockers are multi-statement `execute`, wrong referenced table names, missing `IF NOT EXISTS` on new tables, foreign keys not enabled, and list function returning only the first row.
- User fixed N0. Verified `src/backend/db_l0.py` now runs with valid timezone key, `executescript`, matching `raven_*` foreign-key table names, idempotent table creation, and row-returning list function. Next normal case is `create_query`: a run owns platform-specific query rows.
- User added `create_query` and query listing. Verified N1 passes: a new run stores two platform query rows attached by `run_id`. Cleanup deferred: return query id, use SQL `WHERE` instead of Python filtering, and normalize source labels.
- User added `create_candidates` and candidate listing. Verified N2 passes: a new run stores two platform query rows and two normalized candidate rows, with candidate `query_id` pointing to the created YouTube/Reddit query rows. Fake candidate ids/links now include run id to avoid accidental unique collisions across repeated runs.
- User attempted N3 integrity checks. Audit failed: `PRAGMA foreign_keys = ON` was added, but the normal Reddit candidate insertion reuses the YouTube link for the same run, so `UNIQUE(link)` fails accidentally before deliberate fake-query/duplicate checks. Also `create_query`/`create_candidates` catch `IntegrityError` and call `sys.exit()`, which prevents controlled test flow.
- User fixed N3. Verified normal run/query/candidate insertion passes; fake `query_id` fails via foreign-key constraint; duplicate `(source, platform_id)` fails via unique constraint. Next normal case is joined readback: return the Raven-useful view from runs, queries, and candidates.
- User added a joined readback query. Audit result: partial pass. SQL JOIN works, but `list_joined_candidates()` hardcodes run id `38`, so the script creating run `43` prints stale joined rows. Need to parameterize the function with `run_id` before normal cases are done.
- User fixed joined readback. Verified run id `44` returns current joined rows with `http44` and `httpt44`, and integrity checks still pass. Normal Raven DB cases are complete enough; next phase is backend module cleanup before API adapters.
- User moved work toward `src/backend/db.py` and flagged a bad auto import (`from dbm import sqlite3`) likely inserted by editor tooling. Disabled Python auto-import completions and add-missing-imports/organize-imports code actions in workspace `.vscode/settings.json`. Verified `src/backend/db.py` compiles, imports, creates schema, inserts a run/query/candidate, and returns joined rows.

## [2026-04-18] VAULT | Build-First learning protocol
Project: learning
- User asked to make the working learning method primary and to ingest all session insights into the vault.
- Created `wiki/build-first-learning.md` as the canonical protocol: usable project artifacts, maturity levels instead of feature slices, normal vs adaptive cases, agent audit over hidden implementation, and VIBE_DOCING for narrow mechanism gaps.
- Wired Build-First into `wiki/learning-protocol-hub.md`, `wiki/help-protocol.md`, `wiki/pre-wire-protocol.md`, `wiki/vibe-docing.md`, `learning/README.md`, vault `AGENTS.md`, `CLAUDE.md`, `context/me.md`, `context/now.md`, and `index.md`.
- Tightened the critical learn/help flow: architecture gaps are handled before artifact building, small visible mechanism gaps use VIBE_DOCING, and compounding skill work defaults to Build-First usable artifacts.

## [2026-04-18] UTILITY | Raven Python package import fix
Project: raven
- User hit `ModuleNotFoundError: No module named 'src'` while running `src/backend/test/db_test.py` directly.
- Added package markers at `src/__init__.py`, `src/backend/__init__.py`, and `src/backend/test/__init__.py`.
- Verified package-mode execution works with `python3 -m src.backend.test.db_test`; direct path execution still fails because Python starts `sys.path` at `src/backend/test`, not the repo root.
- VIBE_DOCING rule: package imports require running from the repo root with `-m`, or the script must avoid `src.backend...` imports.
- Cleaned `list_run_candidates()` output by aliasing joined columns (`run_target`, `query_source`, `query_text`, `candidate_source`, `candidate_title`, `candidate_link`) so `dict(row)` is readable and no duplicate `source` key hides meaning.
