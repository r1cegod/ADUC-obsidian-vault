---
type: log
title: '2026-05-12'
created: '2026-05-12'
updated: '2026-05-12'
tags:
  - log
  - session
  - project/raven
  - workflow
status: active
lang: en
feeds_into:
  - log.md
  - duc-os/kickstart.md
  - context/hot.md
---
# 2026-05-12

## Growth Contract
- Parent branch: [[log.md]]
- Node role: daily log
- First parent link: [[log.md]]
- Growth trigger: append only durable session outcomes from 2026-05-12; split nothing from a daily log.
- Forbidden contents: raw transcripts, long command output, private journal rewrite, and full implementation traces.
- Source/evidence boundary: this entry records decisions and closeout only; project detail belongs under Raven notes and executable evidence belongs in the repo.

## Session

- Locked [[duc-os/kickstart]] to `RAVEN RANKER TIER 2 SOURCE PACKET BUILD` for 2026-05-12.
- Opened Raven Tier 2 implementation only for the source-packet lane: `transcript_fetcher`, `card_maker`, `wave_reader`, fixtures, tests, observability, docs, and artifact-shape verification.
- Recorded Duc's architecture stance: Duc may freestyle Tier 2 architecture; agent support defaults to raw mechanism blocks, audit, and critical-mistake interrupts only.
- Promoted VIBE_DOCING into the mandatory default for natural-language "how do I do X" requests when the system seam is visible.
- Updated [[development]], [[wiki/operations/learn-operation]], [[wiki/vibe-docing]], [[projects/raven/notes/raven-current-context]], [[duc-os/current]], and [[context/hot]] to reflect the new lane and learning/help behavior.
- Captured first Tier 2 build bug-class: passing Windows-style `D:/...` into WSL/Linux path code creates a literal `D:` folder under the current repo instead of targeting the Windows drive; Raven transcript output paths should be repo/vault-relative or normalized to `/mnt/d/...` when crossing WSL/Windows boundaries.
- Captured Tier 2 transcript API reliability note: `youtube-transcript-api` does not publish a stable numeric request quota because it uses an undocumented YouTube web-client endpoint; treat rate limiting as IP/block risk and start parallel fetches conservatively with low worker count, retry/backoff, idempotent writes, and persisted failure reasons.
- Audited Duc's first `src/backend/data/transcript_fetcher.py` seam. Immediate blocker: import fails because module-level `get_title()` is called without `db/query_id/video_id`; DB schema also has `CREATE TABLE IF NOT EXIST` and `DEFAUT` typos in `transcript_log`. Design risks: module-level DB/API state blocks clean parallelism, transcript variable can remain unset, `get_title` queries wrong column and reads rows incorrectly, output path is hardcoded to vault root instead of injectable, and the fetcher currently returns no explicit status/path object for downstream `card_maker`.
