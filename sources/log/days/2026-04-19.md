# 2026-04-19

Summary: Audited Raven DB contract test and identified remaining assert/failure-case gaps before API adapter work.

## Activity

## [2026-04-19] AUDIT | Raven DB contract test
Project: raven
- User completed a plain-Python DB contract script but skipped integrity-error assertions due to not seeing the pattern.
- Audited `src/backend/test/db_test.py`: script runs and inserts/readbacks two rows, but current field assertions use `value == "x" and "y"`, which does not check both platform rows.
- Identified that catching `AssertionError` at the top level makes the command print a failure while still exiting successfully, weakening the contract.
- Next action: replace paired-row checks with set/list-based assertions and add a helper that expects `sqlite3.IntegrityError` for duplicate link/platform or invalid foreign-key cases.
- User hit `candidate link error`; audit showed the DB returned both links correctly, but the set comprehensions used `raws[0][...]` inside the loop instead of `raw[...]`, so they only collected the first row repeatedly.
- User then hit `TypeError: list indices must be integers or slices, not str`; audit showed the comprehensions changed to `raws["..."]`, indexing the full list by a string instead of indexing the loop row `raw["..."]`.
- User fixed the row-loop assertions. Verified `python3 -m src.backend.test.db_test` exits cleanly, returns two joined rows, and the duplicate candidate insert triggers the expected `sqlite3.IntegrityError` path.

## [2026-04-19] BUILD | Raven metadata discovery workbench
Project: raven
- User redirected from stepwise lesson mode to a faster build-the-thing mode using `docs/reddit_youtube_api_research_report.md` as the API blueprint.
- Added backend modules: `config.py`, `source_candidate.py`, `query_enricher.py`, `youtube_search.py`, `reddit_search.py`, `sqlite_store.py`, and `discovery_workbench.py`.
- Wired one usable command: `python3 -m src.backend.discovery_workbench "sqlite crash course" --limit 5`.
- Patched `db.py` so low-level candidate inserts raise real `sqlite3.IntegrityError`; duplicate handling now belongs at the store boundary.
- Updated README usage and repo dev log; ignored local SQLite runtime files.
- Verified Python compile, DB contract script, and discovery runner. Runner created run/query logs but inserted zero candidates because `.env` API credential values are empty.
- User clarified active Raven should be built by Duc, not prebuilt by the agent. Moved generated pipeline modules from `src/backend/` to `docs/reference/metadata_discovery_workbench/` as teaching reference only; active backend now contains only DB foundation and tests.

## [2026-04-19] PLAN | Raven backend folder structure
Project: raven
- User asked to structure the backend before building YouTube search.
- Audited active backend: `src/backend/db.py`, empty `src/backend/youtube_search.py`, and `src/backend/test/db_test.py`.
- Recommended a flow-based folder structure: `search/` for API calls, `normalize/` for API JSON to candidate shape, `pipeline/` for orchestration, and current `db.py` left in place until storage pressure is real.

## [2026-04-19] REFERENCE | YouTube search.list raw artifact
Project: raven
- User defined the "steal protocol": inspect finished reference code, then build his own active implementation.
- Added `docs/reference/youtube_search_list_raw.py` as a reference-only YouTube Data API `search.list` artifact: query input, full raw JSON output, no normalization, no SQLite, no Raven pipeline.
- Verified compile and missing-key behavior. Active backend `src/backend/search/youtube_search.py` was not edited.

## [2026-04-19] DOC | YouTube API key setup
Project: raven
- User asked how to get the YouTube API for Raven search work.
- Checked official YouTube Data API docs and clarified that Raven public search needs a YouTube Data API v3 API key, not OAuth.
- Setup path: Google Cloud project -> enable YouTube Data API v3 -> create API key -> restrict it to YouTube Data API v3 -> place value in `.env` as `YOUTUBE_API_KEY` -> run the raw reference artifact.
- Clarified YouTube quota model for Raven: quota is unit-based, `search.list` costs 100 units per call, normal read/list detail calls are much cheaper, and failed/invalid requests still consume at least 1 unit.

## [2026-04-19] LEARN | Absorption protocol on YouTube raw artifact
Project: learning
- User defined "absorption" protocol: active only when interest appears and he wants to study a specific artifact in detail.
- Updated `wiki/build-first-learning.md` with absorption as a non-default deep artifact-disassembly mode.
- Applied absorption to `docs/reference/youtube_search_list_raw.py`: explain every input/output and small function detail, without turning it into active Raven implementation.
- User questioned why the reference artifact manually loads `.env` instead of using `load_dotenv()` and asked whether the API call function is clean. Clarified stdlib reference vs dependency-based active code: `python-dotenv` is cleaner if added as a dependency, while the reference avoided external dependencies.
- User asked for the YouTube `search.list` params doc and raw-response normalization path. Checked official docs and mapped Raven's minimal params plus `items[].id.videoId` / `items[].snippet.*` into candidate fields. Audited active `src/backend/search/youtube_search.py`: it currently mixes DB imports into search, imports nonexistent DB names, and has an invalid URL placeholder.
- User caught a VIBE_DOCING violation: examples reused his active query value. Reinforced `wiki/vibe-docing.md` to ban reuse of Duc's current live query, table values, API values, filenames-under-construction, or candidate data as example values.
- Audited active `src/backend/search/youtube_search.py`: it compiles, but runtime currently fails first on missing installed `python-dotenv` despite `requirements.txt` listing it. Code-level blockers after dependency: recursive `youtube_search()` call, return tuple where dataclass is declared, `items` typo as `item`, SQLite import/catch still in search layer, and `api_get()` return type mismatch.
- User fixed several YouTube search issues: removed DB imports, added dataclass returns in `youtube_call`, fixed `items`, and changed `api_get` to `str | None`. Re-audit still blocked at import because system Python has no `dotenv` installed and no `pip`; remaining code issue is dataclass unpacking (`status_code, raw_response, errors = youtube_call(...)`) instead of reading attributes.
- User cleaned `src/backend/search/youtube_search.py`: dataclass attribute access is fixed, broad exception wrapper is gone, URL and normalization path are coherent, and compile passes. Runtime initially appeared blocked by missing `python-dotenv` because system Python was used.
- User pointed out the repo has a working venv. Verified `.venv/bin/python` imports `dotenv`, compiles `src/backend/search/youtube_search.py`, and successfully runs a live neutral YouTube search returning status 200 plus one normalized candidate.

## [2026-04-19] VAULT | Raven repo state sync
Project: raven
- User requested vault keeping and repo update after YouTube search L1 passed.
- Updated repo README, repo dev log, and daily repo log with active state: SQLite foundation exists; YouTube search + first normalization pass is live-smoke verified; next seam is one YouTube result into SQLite.
- Updated vault `projects/raven/README.md`, active Raven learning session, `context/hot.md`, and `context/now.md` to reflect current state and next action.
- Recorded repo hygiene flag: root `test/` exists but violates Raven's backend test folder rule; do not treat it as accepted structure.

## [2026-04-19] BUILD | Raven requirements and enricher key binding
Project: raven
- Added root `requirements.txt` for the active backend imports: LangChain Core, LangChain OpenAI, LangGraph, Pydantic, and python-dotenv.
- Wired `src/backend/enricher_graph.py` so `ChatOpenAI` reads the custom `ENRICHER_DEV_KEY` value explicitly instead of relying on the default `OPENAI_API_KEY` lookup.
- Installed requirements into the local `.venv` and verified `pip check` plus a smoke import of the enricher LLM.

## [2026-04-19] FIX | Raven venv interpreter
Project: raven
- Rebuilt `/home/r1ceg/Raven/.venv` with copied Python executables instead of symlinked interpreter files to reduce VS Code/Antigravity invalid-interpreter detection.
- Reinstalled Raven `requirements.txt` into the rebuilt venv.
- Updated local `.vscode/settings.json` to point directly at `/home/r1ceg/Raven/.venv/bin/python` and load `${workspaceFolder}/.env`.
- Verified Python 3.12.3, `pip check`, and imports for LangChain OpenAI, LangGraph, Pydantic, and dotenv.

## [2026-04-19] VERIFY | Raven venv reinstall
Project: raven
- Recreated `/home/r1ceg/Raven/.venv` with `python3 -m venv --clear --copies .venv`.
- Reinstalled Raven `requirements.txt` into `/home/r1ceg/Raven/.venv/bin/python`.
- Verified the VS Code configured interpreter path exists, is executable, and matches `sys.executable`.
- Verified Python 3.12.3, `pip check`, active backend dependency imports, and Python extensions for VS Code/Antigravity availability.

## [2026-04-19] FIX | Raven VS Code WSL interpreter path
Project: raven
- User provided VS Code logs showing the Python extension translated `/home/r1ceg/Raven/.venv/bin/python` into invalid Windows path `D:\home\r1ceg\Raven\.venv\bin\python` while workspace was opened via `\\wsl.localhost\Ubuntu\home\r1ceg\Raven`.
- Replaced absolute WSL interpreter and venv paths in `.vscode/settings.json` with workspace-relative paths so Windows-side path probing does not manufacture `D:\home`.
- Added workspace `python.languageServer: Pylance` because logs showed editor support was inactive with language server set to None.
- Revalidated settings JSON, venv executable, `pip check`, and active dependency imports.

## [2026-04-19] FIX | Raven Windows-side Python interpreter
Project: raven
- User observed VS Code/Antigravity selecting interpreters through a Windows `python.exe` shape while Raven's Linux venv only had `.venv/bin/python`.
- Created separate Windows-side editor venv `.venv-win` using Windows Python 3.13.7, leaving the WSL runtime venv `.venv` intact.
- Installed Raven `requirements.txt` into `.venv-win` and verified `pip check` plus active dependency imports.
- Updated `.vscode/settings.json` to use `${workspaceFolder}\.venv-win\Scripts\python.exe` for Windows-side editor validation and added `.venv-win/` to `.gitignore`.
- Confirmed Windows Python can see the UNC interpreter path `\\wsl.localhost\Ubuntu\home\r1ceg\Raven\.venv-win\Scripts\python.exe`.

## [2026-04-19] SETUP | Raven VS Code Remote WSL
Project: raven
- User chose long-term WSL workflow instead of Windows-side `python.exe` interpreter probing.
- Installed Microsoft VS Code Remote WSL extension `ms-vscode-remote.remote-wsl` locally; verified version `0.104.3`.
- Launched Raven with `code --remote wsl+Ubuntu /home/r1ceg/Raven` and verified remote WSL server has Python, Pylance, and Remote WSL extensions under `~/.vscode-server/extensions`.
- Reverted Raven workspace interpreter path to `${workspaceFolder}/.venv/bin/python` for the WSL extension host.
- Removed temporary `.venv-win` generated for the previous Windows-side workaround.
- Antigravity WSL server setup still fails from its CLI with a 404 for its own server commit, so VS Code Remote WSL is the stable path for now.

## [2026-04-19] SETUP | VS Code replaces Antigravity
Project: raven
- User decided to dump Antigravity and use VS Code Remote WSL as the long-term editor path.
- Migrated useful Antigravity user settings into VS Code user settings while excluding broken PathFinder Windows venv paths and Antigravity-only runtime keys.
- Matched Antigravity visual state in VS Code settings/storage: dark theme background `#1f1f1f`, chrome `#181818`, border `#2b2b2b`, left sidebar, activity bar startup width `42`, sidebar width `170`, auxiliary/chat bar width `525`, auxiliary bar visible.
- Installed/mirrored useful extensions into VS Code/WSL: Remote WSL, Python, Pylance, GitLens, Git History, Go, clangd, Docker/Containers, PowerShell, Java pack, Ruby LSP.
- Verified VS Code WSL server updated successfully, Raven opens through `code --remote wsl+Ubuntu /home/r1ceg/Raven`, JSON settings are valid, and Raven `.venv` passes `pip check`.
